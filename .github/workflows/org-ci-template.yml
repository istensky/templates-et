name: build with reusable template as starter
on:
  push:
    branches: [ "main" ]
  pull_request:
   branches: [ "main" ]
jobs:
  build-job:
    runs-on: ubuntu-latest

    steps:

      - name: step 1
        run:  echo "first step"

      - name: step 2
        run:  echo "second step"
