on:
  push:
    branches: [ "main" ]
  pull-request:
   branches: [ "main" ]
jobs:
  build-job:
    run-on: ubuntu-latest

    steps:

      - name: step 1
        run:  echo "first step"

      - name: step 2
        run:  echo "second step"
